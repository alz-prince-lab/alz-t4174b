name: 99 TEMP Force Unlock
on:
  workflow_dispatch:
    inputs:
      lock_id:
        description: 'Terraform state lock ID to break'
        required: true
        type: string

jobs:
  force_unlock:
    runs-on: self-hosted
    environment: alz-t4174b-apply
    permissions:
      id-token: write
      contents: read
    env:
      ARM_CLIENT_ID: "${{ vars.AZURE_CLIENT_ID }}"
      ARM_SUBSCRIPTION_ID: "${{ vars.AZURE_SUBSCRIPTION_ID }}"
      ARM_TENANT_ID: "${{ vars.AZURE_TENANT_ID }}"
      ARM_USE_AZUREAD: true
      ARM_USE_OIDC: true
    steps:
      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Install Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false
          terraform_version: '1.14.9'

      - name: Terraform Init
        shell: pwsh
        run: |
          $tmpDir = "${{ runner.temp }}/force-unlock"
          New-Item -ItemType Directory -Path $tmpDir -Force | Out-Null
          Set-Location $tmpDir
          @'
          terraform {
            backend "azurerm" {}
          }
          '@ | Out-File -FilePath "backend.tf" -Encoding utf8
          terraform init `
            -backend-config="resource_group_name=${{ vars.BACKEND_AZURE_RESOURCE_GROUP_NAME }}" `
            -backend-config="storage_account_name=${{ vars.BACKEND_AZURE_STORAGE_ACCOUNT_NAME }}" `
            -backend-config="container_name=${{ vars.BACKEND_AZURE_STORAGE_ACCOUNT_CONTAINER_NAME }}" `
            -backend-config="key=terraform.tfstate"

      - name: Force Unlock
        shell: pwsh
        run: |
          Set-Location "${{ runner.temp }}/force-unlock"
          terraform force-unlock -force "${{ inputs.lock_id }}"
